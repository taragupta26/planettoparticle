# Copy this file to `.env.local` and fill in the value, then restart the dev server.

# Free global real-time AIS (ship positions) from AISStream.io.
# Register a free key at https://aisstream.io and paste it below.
# Without this, the "Vessels" layer shows a data gap (no fabricated positions).
AISSTREAM_API_KEY=
