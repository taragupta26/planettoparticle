# dependencies
/node_modules

# next.js build output
/.next
/out
next-env.d.ts
tsconfig.tsbuildinfo

# secrets — never commit (keep .env.local.example as the template)
.env.local
.env*.local

# local DuckDB build artifact (regenerate via scripts/build-db.ts)
/data/p2p.duckdb

# os / editor
.DS_Store
*.log
